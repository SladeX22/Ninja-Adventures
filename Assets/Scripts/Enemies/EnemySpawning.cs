using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemySpawning : MonoBehaviour
{
    // [SerializeField] List<GameObject> enemies;
    // [SerializeField] int maxEnemies;

    // [SerializeField] Vector2 spawnRange;
    // [SerializeField] float spawnTime;
    // int timeCounter;

    // private void Awake() {
    //     timeCounter = spawnTime;
    // }

    // private void Start() {
    //     //while()
    //     int index = Random.Range(0, enemies.Length());

    //     SpawnEnemy(enemies[index]);
    // }

    // private void SpawnEnemy(GameObject go)
    // {
    //     Instantiate(go, new Vector3(Random.Range(0, spawnRange.x), Random.Range(0, spawnRange.y)), transform.rotation);
    // }
}
